{
  "name": "harness",
  "version": "0.1.0",
  "description": "Runs every adapter on the same input and reports their segments with representative frames.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "^1",
    "@types/node": "^22",
    "typescript": "^5"
  }
}
