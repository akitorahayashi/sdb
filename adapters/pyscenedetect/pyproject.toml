[project]
name = "pyscenedetect-adapter"
version = "0.1.0"
description = "Emits the normalized segments contract from PySceneDetect's content detector."
requires-python = ">=3.10"
dependencies = ["scenedetect>=0.7"]

[tool.uv]
package = false
